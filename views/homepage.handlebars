{{#each posts}}
  <article>
    <header>
      <h2>{{this.title}}</h2>
    </header>
    <section>
      <p>{{this.content}}</p>
    </section>
    <footer>
      <p>Posted by {{this.user.username}} on {{this.createdAt}}</p>
    </footer>
  </article>
{{/each}}
